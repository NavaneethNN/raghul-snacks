import { boolean, integer, numeric, pgTable, serial, text, timestamp, uniqueIndex } from "drizzle-orm/pg-core";

const createdAt = timestamp("created_at", { withTimezone: true }).defaultNow().notNull();

export const categories = pgTable("categories", { id: serial("id").primaryKey(), name: text("name").notNull(), slug: text("slug").notNull(), description: text("description"), image: text("image"), createdAt }, (table) => [uniqueIndex("categories_slug_unique").on(table.slug)]);
export const products = pgTable("products", { id: serial("id").primaryKey(), name: text("name").notNull(), slug: text("slug").notNull(), description: text("description").notNull(), ingredients: text("ingredients"), price: numeric("price", { precision: 10, scale: 2 }).notNull(), offerPrice: numeric("offer_price", { precision: 10, scale: 2 }), weight: text("weight").notNull(), categoryId: integer("category_id").references(() => categories.id), image: text("image"), stock: integer("stock").notNull().default(0), featured: boolean("featured").notNull().default(false), bestseller: boolean("bestseller").notNull().default(false), createdAt }, (table) => [uniqueIndex("products_slug_unique").on(table.slug)]);
export const combos = pgTable("combos", { id: serial("id").primaryKey(), title: text("title").notNull(), slug: text("slug").notNull(), price: numeric("price", { precision: 10, scale: 2 }).notNull(), discount: numeric("discount", { precision: 10, scale: 2 }).notNull().default("0"), image: text("image"), createdAt }, (table) => [uniqueIndex("combos_slug_unique").on(table.slug)]);
export const comboItems = pgTable("combo_items", { id: serial("id").primaryKey(), comboId: integer("combo_id").notNull().references(() => combos.id, { onDelete: "cascade" }), productId: integer("product_id").notNull().references(() => products.id), quantity: integer("quantity").notNull().default(1) });
export const customers = pgTable("customers", { id: serial("id").primaryKey(), name: text("name").notNull(), phone: text("phone").notNull(), email: text("email"), createdAt }, (table) => [uniqueIndex("customers_phone_unique").on(table.phone)]);
export const customerAccounts = pgTable("customer_accounts", { id: serial("id").primaryKey(), name: text("name").notNull(), email: text("email").notNull(), passwordHash: text("password_hash"), googleId: text("google_id"), createdAt }, (table) => [uniqueIndex("customer_accounts_email_unique").on(table.email), uniqueIndex("customer_accounts_google_id_unique").on(table.googleId)]);
export const orders = pgTable("orders", { id: serial("id").primaryKey(), orderNumber: text("order_number").notNull(), customerId: integer("customer_id").references(() => customers.id), accountId: integer("account_id").references(() => customerAccounts.id), customerName: text("customer_name").notNull(), phone: text("phone").notNull(), email: text("email"), address: text("address").notNull(), city: text("city").notNull(), state: text("state").notNull(), pincode: text("pincode").notNull(), total: numeric("total", { precision: 10, scale: 2 }).notNull(), couponCode: text("coupon_code"), discount: numeric("discount", { precision: 10, scale: 2 }).notNull().default("0"), paymentStatus: text("payment_status").notNull().default("pending"), orderStatus: text("order_status").notNull().default("placed"), paymentOrderId: text("payment_order_id"), shiprocketOrderId: text("shiprocket_order_id"), shipmentId: text("shipment_id"), awbCode: text("awb_code"), shippingStatus: text("shipping_status").notNull().default("pending"), createdAt }, (table) => [uniqueIndex("orders_number_unique").on(table.orderNumber)]);
export const settings = pgTable("settings", { id: serial("id").primaryKey(), key: text("key").notNull().unique(), value: text("value").notNull(), description: text("description"), updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull() });
export const orderItems = pgTable("order_items", { id: serial("id").primaryKey(), orderId: integer("order_id").notNull().references(() => orders.id, { onDelete: "cascade" }), productId: integer("product_id").references(() => products.id), name: text("name").notNull(), quantity: integer("quantity").notNull(), price: numeric("price", { precision: 10, scale: 2 }).notNull() });
export const coupons = pgTable("coupons", { id: serial("id").primaryKey(), code: text("code").notNull(), name: text("name"), description: text("description"), discountType: text("discount_type").notNull(), value: numeric("value", { precision: 10, scale: 2 }).notNull(), maxDiscount: numeric("max_discount", { precision: 10, scale: 2 }), minOrderValue: numeric("min_order_value", { precision: 10, scale: 2 }).notNull().default("0"), validFrom: timestamp("valid_from", { withTimezone: true }), validUntil: timestamp("valid_until", { withTimezone: true }), totalUsage: integer("total_usage"), perCustomer: integer("per_customer"), applicableProducts: text("applicable_products").array(), applicableCategories: text("applicable_categories").array(), applyTo: text("apply_to").notNull().default("entire_store"), firstPurchase: boolean("first_purchase").notNull().default(false), publicCoupon: boolean("public_coupon").notNull().default(true), notes: text("notes"), active: boolean("active").notNull().default(true), createdAt }, (table) => [uniqueIndex("coupons_code_unique").on(table.code)]);
export const reviews = pgTable("reviews", { id: serial("id").primaryKey(), productId: integer("product_id").references(() => products.id), customerName: text("customer_name").notNull(), rating: integer("rating").notNull(), content: text("content").notNull(), approved: boolean("approved").notNull().default(false), createdAt });
export const banners = pgTable("banners", { id: serial("id").primaryKey(), eyebrow: text("eyebrow").notNull().default(""), title: text("title").notNull(), subtitle: text("subtitle"), offerText: text("offer_text"), couponCode: text("coupon_code"), buttonText: text("button_text").notNull().default("Shop Now"), validityText: text("validity_text"), image: text("image"), href: text("href").notNull().default("/shop"), active: boolean("active").notNull().default(true), createdAt });
export const announcements = pgTable("announcements", { id: serial("id").primaryKey(), text: text("text").notNull(), icon: text("icon"), active: boolean("active").notNull().default(true), order: integer("order").notNull().default(0), createdAt });
